#include <stdio.h>
#include <curl/curl.h>



int main (){
    /*
    a). create easy handle for transfer
    b). set options for transfer
    c). perform transfer
    d). cleanup after transfer
    */

    printf("Hello World!");
}